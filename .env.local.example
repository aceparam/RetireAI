# Base URL of the RetireAI NestJS API.
# When set, the app shows "Sign in with Google" and syncs your plan to the cloud.
# Leave unset to run the app fully offline (localStorage only).
NEXT_PUBLIC_API_URL=http://localhost:4000
